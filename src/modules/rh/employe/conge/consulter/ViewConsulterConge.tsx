import { Section } from 'waxant';
import EtatConge from './element/EtatConge';

const ViewConsulterConge = () => {
    //
    return (
        <Section>
            <EtatConge />
        </Section>
    );
};

export default ViewConsulterConge;